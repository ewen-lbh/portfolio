extract colors:
  enabled: true

make thumbnails:
  enabled: true
  sizes:
  - 100
  - 400
  - 600
  - 1200
  file name template: <work id>/<block id>@<size>.webp

media:
  at: media/

build metadata file: .lastbuild.yaml
scattered mode folder: .portfoliodb
