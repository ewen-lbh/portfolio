package pages

import "github.com/ortfo/db"
import "github.com/ewen-lbh/portfolio/components"

templ Index(db ortfodb.Database, language string) {
	@components.GalleryPage(db.WorksSlice(), language, "Hi! I'm Ewen Le Bihan.", false) {
		<p i18n class="intro">These are my creations.</p>
	}
}
